<h2><%= @artist.name %></h2>
<ul>
    <li><%= @artist.title %></li>
    <li><%= @artist.age %></li>
</ul>
<p>Instruments:</p>
<ul><% @artist.instruments.each do |instrument| %>
    <li><%= instrument.name %></li>
    </ul>
    <% end %>
